#include "main.h"

/**
 * main - tests that the function positive_or_negative()
 * gives the correct output when given a case of 0.
 *
 * Return: Always 0
 */
int main(void)
{
	positive_or_negative(0);
	return (0);
}
