#include <stdio.h>

/**
 * main - Entry point function
 * It prints out a string to the stdout using printf
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
